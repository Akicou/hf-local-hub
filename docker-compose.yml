version: '3.8'

services:
  hf-local:
    build: .
    container_name: hf-local-server
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - HF_LOCAL_PORT=8080
      - HF_LOCAL_DATA_DIR=/app/data
    restart: unless-stopped
